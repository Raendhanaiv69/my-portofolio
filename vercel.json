{
  "version": 2,
  "framework": null,
  "functions": {
    "api/index.php": {
      // HARUS menggunakan runtime komunitas vercel-php, BUKAN @vercel/php
      "runtime": "vercel-php@0.7.1" 
    }
  },
  "routes": [
    // Rute untuk aset statis
    {
      "src": "/(css|js|images|svg|build)/(.*)",
      "dest": "/public/$1/$2"
    },
    // Rute utama ke Serverless Function
    {
      "src": "/(.*)",
      "dest": "/api/index.php"
    }
  ],
  "buildCommand": "php /usr/local/bin/composer install --no-dev && npm install && npm run build",
  "outputDirectory": "public",
  "env": {
    "APP_ENV": "production",
    "APP_DEBUG": "false",
    "LOG_CHANNEL": "stderr",
    "SESSION_DRIVER": "cookie",
    "CACHE_DRIVER": "file",
    "APP_CONFIG_CACHE": "/tmp/config.php",
    "VIEW_COMPILED_PATH": "/tmp/views"
  }
}
